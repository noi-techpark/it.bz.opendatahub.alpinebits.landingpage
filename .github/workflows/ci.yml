name: CI

on: push


jobs:
  build:
    runs-on: ubuntu-20.04

    steps:
      - name: clone into repo
        uses: actions/checkout@v2

      - name: test
        run: cd src && test -f index.html
